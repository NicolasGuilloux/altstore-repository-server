# Server Configuration
# The IP address the server should listen on (0.0.0.0 for all interfaces)
LISTEN_URL=0.0.0.0

# The port the server should listen on
LISTEN_PORT=9123

# The external base URL where the AltStore repository is accessible
# This should match your public domain or short URL
EXTERNAL_BASE_URL=https://altstore.example.com

# Directory containing app IPA files (relative or absolute path)
APPS_DIR=apps
